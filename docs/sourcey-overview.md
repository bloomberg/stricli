# Stricli TypeScript API

This reference is generated from Stricli's TypeScript source during the same
build that publishes the main documentation site. It covers the public APIs for
`@stricli/core` and `@stricli/auto-complete` with pinned source links.

Use the navigation to browse classes, functions, interfaces, type aliases, and
variables. The root `llms.txt` and `llms-full.txt` files provide compact and
expanded machine-readable indexes of the same generated pages.
